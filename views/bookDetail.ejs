<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <meta charset="UTF-8">
        <title>
            <%= book.title %> - Acervo Digital
        </title>
        <link rel="stylesheet" href="/css/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>

    <body class="body-default">
        <div class="top-bar">
            <%= book.title %>
        </div>
        <div class="container mt-4">
            <h2 class="mb-4">
                <%= book.title %>
            </h2>

            <div class="row">
                <div class="col-md-4">
                    <% if (book.cover) { %>
                        <img src="<%= book.cover %>" alt="Capa do Livro" class="img-fluid rounded">
                        <% } else { %>
                            <img src="/img/placeholder.png" alt="Sem capa" class="img-fluid rounded">
                            <% } %>
                </div>

                <div class="col-md-8">
                    <ul class="list-group">
                        <li class="list-group-item"><strong>Autor:</strong>
                            <%= book.author %>
                        </li>
                        <li class="list-group-item"><strong>Editora:</strong>
                            <%= book.publisher %>
                        </li>
                        <li class="list-group-item"><strong>Ano:</strong>
                            <%= book.year %>
                        </li>
                        <li class="list-group-item"><strong>ISBN:</strong>
                            <%= book.isbn %>
                        </li>
                        <li class="list-group-item"><strong>Categoria:</strong>
                            <%= book.category %>
                        </li>
                        <li class="list-group-item"><strong>Quantidade:</strong>
                            <%= book.quantity %>
                        </li>
                        <li class="list-group-item"><strong>Descrição:</strong>
                            <%= book.description %>
                        </li>
                    </ul>

                    <div class="mt-3">
                        <a href="/books/edit/<%= book.id %>" class="btn btn-dark">Editar</a>
                        <form action="/books/delete/<%= book.id %>" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">Excluir</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <a href="/books" class="btn w-100 mt-3">Voltar para Livros</a>
        </div>
        </div>
    </body>

</html>