<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <meta charset="UTF-8" />
        <title>Meus Livros - Sistema Biblioteca</title>
        <link rel="stylesheet" href="/styles.css" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    </head>

    <body class="bg-light">
        <div class="container mt-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Meus Livros</h2>
                <div>
                    <a href="/books/create" class="btn btn-success me-2">➕ Novo Livro</a>
                    <a href="/logout" class="btn btn-outline-danger">Sair</a>
                </div>
            </div>

            <% if (books.length===0) { %>
                <div class="alert alert-info">Você ainda não cadastrou nenhum livro.</div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>Título</th>
                                    <th>Autor</th>
                                    <th>Editora</th>
                                    <th>Ano</th>
                                    <th>ISBN</th>
                                    <th>Quantidade</th>
                                    <th>Categoria</th>
                                    <th>Descrição</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% books.forEach(book=> { %>
                                    <tr>
                                        <td>
                                            <%= book.title %>
                                        </td>
                                        <td>
                                            <%= book.author %>
                                        </td>
                                        <td>
                                            <%= book.publisher %>
                                        </td>
                                        <td>
                                            <%= book.year %>
                                        </td>
                                        <td>
                                            <%= book.isbn %>
                                        </td>
                                        <td>
                                            <%= book.quantity %>
                                        </td>
                                        <td>
                                            <%= book.category %>
                                        </td>
                                        <td>
                                            <%= book.description %>
                                        </td>
                                        <td>
                                            <a href="/books/edit/<%= book.id %>" class="btn btn-warning btn-sm">✏️</a>
                                            <a href="/books/delete/<%= book.id %>" class="btn btn-danger btn-sm"
                                                onclick="return confirm('Tem certeza que deseja excluir este livro?')">🗑️</a>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                    </div>
                    <% } %>
        </div>
    </body>

</html>